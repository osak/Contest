//Name: 
//Level: 
//Category: 
//Note: 

/**
 *
 */
#include <iostream>

using namespace std;

bool solve(bool first) {
    return true;
}

int main() {
    cin.tie(0);
    ios::sync_with_stdio(0);
    cout.setf(ios::fixed);
    cout.precision(10);

    bool first = true;
    while(solve(first)) {
        first = false;
    }
    return 0;
}
