#include <iostream>
#include <string>
#include <map>
#include <algorithm>

using namespace std;

bool winning(const vector<int> &list, const string &target) {
    str = "";
    for(vector<int>::const_iterator it = list.begin(); it != list.end(); ++it) {


int main() {

