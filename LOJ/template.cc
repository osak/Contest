#include <iostream>

using namespace std;

ostream& ansout(int c) {
    return cout << "Case " << c << ": ";
}

int main() {
    ios::sync_with_stdio(0);

    int T;
    cin >> T;
    for(int CASE = 1; CASE <= T; ++CASE) {
    }

    return 0;
}
